# Regex tester module (DevTools_regex_tester)

target_sources(DevTools_regex_tester PRIVATE
    # Core
    core/regex_tool.h core/regex_tool.cpp
    # GUI
    gui/regex_tester_gui.h gui/regex_tester_gui.cpp
)
